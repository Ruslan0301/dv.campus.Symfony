<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        <sectin class = "header">
            <ul class="menu">
               <li>
                   <a href="{{ path('check_list_list_by_category', {'categoryId': 1}) }}">My Summer weekeds</a>
                   <a href="{{ path('check_list_list_by_category', {'categoryId': 2}) }}">My favorite books review</a>
                   <a href="{{ path('check_list_list_by_category', {'categoryId': 3}) }}">My friend hobbies</a>
               </li>
            </ul>
        </sectin>
        {% block body %}{% endblock %}
    </body>
</html>
